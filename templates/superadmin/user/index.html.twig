{% extends 'base.html.twig' %}

{% block body %}
    <h1>Gestion des utilisateurs</h1>
    <ul>
        {% for user in users %}
        <li>
            {{ user.email }} - Rôle(s) : {{ user.roles|join(', ') }}
            <a href="{{ path('app_superadmin_change_role', {id: user.id}) }}">Attribuer le role admin</a>
        </li>
        {% endfor %}
    </ul>
{% endblock %}