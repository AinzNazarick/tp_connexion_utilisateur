{% extends 'base.html.twig' %}

{% block title %}Voiture{% endblock %}

{% block body %}
    {%  if is_granted('ROLE_USER') %}
        <h1>Voiture</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voiture.id }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ voiture.model }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ voiture.price }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('app_voiture_user_index') }}">back to list</a>
    {% endif %}
{% endblock %}
