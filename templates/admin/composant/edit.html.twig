{% extends 'base.html.twig' %}

{% block title %}Edit Composant{% endblock %}

{% block body %}
    {%  if is_granted('ROLE_ADMIN') %}
        <h1>Edit Composant</h1>

        {{ include('admin/composant/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_composant_index') }}">back to list</a>

        {{ include('admin/composant/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
