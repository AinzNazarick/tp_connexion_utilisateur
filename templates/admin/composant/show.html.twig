{% extends 'base.html.twig' %}

{% block title %}Composant{% endblock %}

{% block body %}
    {%  if is_granted('ROLE_ADMIN') %}
        <h1>Composant</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ composant.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ composant.name }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_composant_index') }}">back to list</a>

        <a href="{{ path('app_composant_edit', {'id': composant.id}) }}">edit</a>

        {{ include('admin/composant/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
